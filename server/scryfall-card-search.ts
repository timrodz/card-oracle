"use server";

// Docs: https://scryfall.com/docs/api/cards/search
const endpoint = "https://api.scryfall.com/cards/search";

export async function ScryfallApiSearchCards(query: string): Promise<string> {
  return "";
}
